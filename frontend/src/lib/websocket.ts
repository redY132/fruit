// WebSocket client wrapper (connect, send, reconnect)
// TODO: implement auto-reconnect with exponential backoff
export class GameWebSocket {
  connect(_url: string) {}
  send(_msg: unknown) {}
  disconnect() {}
}
